There are two ways to run JS code
1. Inside Browser
	1. inside HTML
	2. separate JS file
2. Outside Browser

Now go to `Program Examples/first-program` directory and copy the path to index.html file and paste it to the browser and try to make/ break every file and every line of the code and keep hitting refresh after every change to the code